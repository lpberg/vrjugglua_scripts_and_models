



local function visitNode(root)		
	root_name = root:getName()
	for i = 1,root:getNumChildren(),1 do
		local child_name = root.Child[i]:getName()
		--create childnode
		--add edge between root and child
	end
	for i = 1,root:getNumChildren(),1 do
		local child_name = root.Child[i]:getName()
		--create childnode
		--add edge between root and child
	end
end

visitNode(root)